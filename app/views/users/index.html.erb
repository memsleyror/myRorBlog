<h1>This is the Users#index View</h1>


<div class="users">
	<% @users.each do |user| %>
		<div class="user">
			<span class="name">
				<%= link_to user.last_name, user %>
			</span>
			(<%= user.email %>)

		</div>
	<% end %>	
</div>	

